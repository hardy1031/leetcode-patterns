## Reflection

Today I tried the fixed-size sliding window pattern for the first time. I had initially approached it with a variable-size mindset, so it didn't work at first. Hopefully I'll be able to recognize the fixed-size pattern more quickly next time. By solving two fixed-size sliding window problems, I aimed to train myself to distinguish this pattern. A for loop works well here because the window always moves by exactly one step per iteration.
