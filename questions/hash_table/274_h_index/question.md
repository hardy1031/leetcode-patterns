# 274. H-Index

Given an array of integers `citations` where `citations[i]` is the number of citations a researcher received for their i-th paper, return the researcher's h-index.

The h-index is defined as the maximum value of `h` such that the given researcher has published at least `h` papers that have each been cited at least `h` times.

**Example 1:** citations = [3,0,6,1,5] → 3 (3 papers have at least 3 citations each)
**Example 2:** citations = [1,3,1] → 1

**Constraints:** n == citations.length, 1 ≤ n ≤ 5000, 0 ≤ citations[i] ≤ 1000.

## Links
- [LeetCode](https://leetcode.com/problems/h-index/)
