# Analysis: 1071. Greatest Common Divisor of Strings

## Layer 3 — Problem-solving Pattern
**Pattern:** Math (GCD applied to string lengths)
If str1 + str2 == str2 + str1, a common divisor exists → its length = gcd(len(str1), len(str2)).

## Layer 2 — Algorithms & Techniques
- GCD (Euclidean algorithm) on string lengths
- Divisibility check via string concatenation comparison

## Layer 1 — Data Structures
- String
